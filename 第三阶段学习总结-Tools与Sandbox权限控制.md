# 第三阶段学习总结：Tools 与 Sandbox 权限控制

## 1. 这一阶段学到了什么

这一阶段的核心目标，是理解 multi-agent 的关键不只是“有多个角色”，而是“不同 Agent 拥有不同能力边界”。

核心结论：

`多 Agent 设计的关键，不是谁会说什么，而是谁真正能做什么，以及系统如何强制限制它只能做这些。`

## 2. 关键概念总结

### 2.1 多 Agent 的核心不是多角色，而是多能力边界

如果一个系统里所有 Agent 都：

- 能读同样的信息
- 能写同样的内容
- 能执行同样的命令
- 能调用同样的外部工具
- 能代表用户做同样的动作

那它通常只是“换了几个名字的单 Agent”。

成熟的多 Agent 设计，关键在于：

- 不同 Agent 拿到不同上下文
- 不同 Agent 拥有不同工具
- 不同 Agent 拥有不同权限
- 不同 Agent 被限制在不同职责范围内

### 2.2 Tools 决定 Agent 真正能做什么

前两阶段更多解决的是：

- 它知道什么
- 它在哪工作

这一阶段补上的是：

- 它真正能做什么

例如，同样都知道一份 PRD：

- 一个 Agent 只能读并总结
- 一个 Agent 可以起草文档
- 一个 Agent 可以执行命令
- 一个 Agent 可以对外发送内容

这些差异本质上都落在工具和权限上。

### 2.3 为什么工具要做 allow / deny

工具本身就是权限。

因此，工具不应该默认平均发放，而应该按职责控制。

主要原因有三类：

- `安全`
  防止越权访问、误操作、信息泄露

- `稳定性`
  减少无关操作，避免系统行为失控

- `职责清晰`
  让 Agent 聚焦自己的角色，不滥用能力

更成熟的设计通常不是只有 `allow / deny`，而是：

- `allow`
  低风险工具，默认开放

- `ask`
  中风险工具，需要审批后开放

- `deny`
  高风险工具，默认禁止

这就是权限渐进开放的一种体现。

### 2.4 Sandbox 不只是安全壳

`sandbox` 不只是为了“防攻击”，它还在做边界落地。

它通常同时解决三类问题：

- `安全问题`
  限制文件系统、网络、系统资源访问

- `稳定性问题`
  防止 Agent 到处乱改、乱跑、乱调用

- `职责问题`
  把“谁能做什么”真正落到执行层

所以，sandbox 本质上是：

`一组被系统强制执行的运行限制。`

### 2.5 Workspace 和 Sandbox 不是一回事

二者经常一起出现，但概念不同。

- `workspace`
  更偏工作范围、执行场地、可接触世界

- `sandbox`
  更偏系统施加的行为限制、护栏和访问规则

一句话区分：

`workspace 决定它面对什么世界，sandbox 决定它在这个世界里能做什么。`

### 2.6 工具隔离和凭据隔离不是一回事

这两个概念必须分开理解。

- `工具隔离`
  控制的是 Agent 有没有某类动作能力

  例如：
  - 能不能执行 shell
  - 能不能写文件
  - 能不能调用浏览器

- `凭据隔离`
  控制的是 Agent 是否带着某种授权身份去做动作

  例如：
  - 有没有 Jira token
  - 有没有 Slack 登录态
  - 有没有内部 API key

所以，一个 Agent 完全可能是这种状态：

- 能打开浏览器，但没有登录态
- 能发请求，但没有生产环境 token
- 能写草稿，但不能正式提交

### 2.7 大模型负责提出动作，程序负责裁决能不能执行

这一阶段最关键的工程认识之一是：

`权限判断不应该交给大模型自己判断，而应该由程序或运行时系统来判断。`

成熟系统通常是这样工作的：

1. 大模型根据当前上下文，提出下一步动作建议
2. 框架/运行时接收工具调用请求
3. 系统根据权限、sandbox、策略、审批规则做判断
4. 决定允许、拒绝或要求审批
5. 再把结果返回给模型

因此：

- 大模型负责“想做什么”
- 程序负责“实际上能不能做”

## 3. 常见疑问 1：Sandbox 为什么是系统强制执行的

如果 sandbox 只是写在 prompt 或文档里，那它不算真正的强制执行。

真正的强制执行，通常依赖以下几层：

- 工具入口受控
  模型不能直接碰系统，只能通过框架暴露的工具动作

- 权限检查
  每次工具调用前，系统检查是否允许

- 文件系统/容器/OS 级限制
  限制目录访问、网络访问、系统命令、敏感路径

- 审批门
  高风险动作必须人工确认后才能继续

所以真正可靠的是：

`prompt 在描述边界，sandbox 在执行边界。`

## 4. 常见疑问 2：大模型有幻觉，Sandbox 还能拦住吗

能拦住“执行越权”，但拦不住“模型乱想”。

这两件事要分开：

- `认知错误`
  模型可能猜错、记错、误判、幻想某个工具存在

- `执行越权`
  模型试图调用未授权工具、访问未授权路径、执行未授权动作

只要 sandbox 和权限系统是真的，模型即使出现幻觉，也只能：

- 提出错误请求
- 调用失败
- 被拒绝
- 被要求审批

它不能因为自己“以为可以”，就真的越权成功。

一句话总结：

`幻觉可以让模型想做错事，但不应该让它做成错事。`

## 5. 一个最简权限分层表

```md
| Agent | 可读 | 可写 | 可执行 | 可外呼/代发 | 备注 |
|------|------|------|--------|-------------|------|
| general | 是 | 否 | 否 | 否 | 总入口，负责分发与汇总 |
| researcher | 是 | 可写草稿 | 有限 | 否 | 负责检索、整理、归纳 |
| operator | 是 | 是 | 是 | 视审批而定 | 风险较高，必须有审批机制 |
```

这个表的价值不在于名字本身，而在于：

`权限应该跟职责绑定，而不是默认平均发放。`

## 6. 这一阶段是否通过的判断标准

如果你现在已经能稳定区分下面这些点，就算这一阶段过关：

- 多 Agent 的核心不是多角色，而是多能力边界
- 工具本身就是权限，不该默认发给所有 Agent
- `allow / ask / deny` 比单纯 `allow / deny` 更成熟
- sandbox 不只是安全壳，也是行为边界的落地机制
- `workspace`、`tools`、`sandbox` 是不同维度
- `工具隔离` 和 `凭据隔离` 不是一回事
- 权限判断应该由程序/运行时执行，而不是靠模型自觉

## 7. 下一阶段入口

第四阶段将进入：

`Multi-Agent Routing`

重点会从“谁能做什么”进一步推进到：

`任务应该怎么分发、结果怎么回收、协调层应该薄到什么程度。`
