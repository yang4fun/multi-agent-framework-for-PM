---
layout: note
title: 第二阶段学习总结：上下文与工作空间
summary: 理解 workspace、context 和最小必要暴露之间的关系。
permalink: /context-and-workspace/
---

# 第二阶段学习总结：上下文与工作空间

## 1. 这一阶段学到了什么

这一阶段的核心目标，是理解为什么成熟的 multi-agent 框架不会让所有 Agent 默认共享全部信息。

核心结论：

`成熟的多 Agent 设计，不靠信息全量共享，而靠上下文分层和最小必要暴露。`

换句话说：

`不是让每个 Agent 知道得越多越好，而是让每个 Agent 只拿到完成当前任务真正需要的信息。`

## 2. 关键概念总结

### 2.1 上下文不是越多越好

在多 Agent 系统里，给每个 Agent 默认塞入完整背景，通常不是好设计。

主要问题有三类：

- 信息噪音增加，推理更容易跑偏
- Agent 更难聚焦自己的职责和目标
- 敏感信息更容易被无差别暴露

所以，问题不只是效率，而是边界设计本身会失真。

### 2.2 Workspace 和 Context 不是一回事

这两个概念很接近，但必须分开。

- `workspace`
  更偏执行环境、工作边界、可接触范围

- `context`
  更偏某一次任务里，实际带入推理过程的信息

一句话区分：

`workspace 是可能的信息来源和操作范围，context 是本次真正放进脑子里的内容。`

### 2.3 Workspace 是相对稳定的空间边界

`workspace` 一般在 Agent 定义清楚后会相对固定。

它回答的是：

- 这个 Agent 默认能读什么
- 它能在哪些范围内工作
- 它能不能写内容
- 它能接触哪些项目资源

它可以调整，但通常不会随着每一轮请求任意变化。

### 2.4 Context 是一次任务中的实际注入内容

`context` 更接近一次请求中的有效上下文。

它可能来自：

- 用户当前输入
- workspace 中选出来的部分内容
- 历史会话
- 上游 Agent 输出
- 外部检索结果

所以，`context` 往往是多个来源组合后的结果，而不只是 workspace 的全部内容。

### 2.5 不同类型的信息应该分层

第二阶段最重要的设计原则之一，就是不要把不同性质的信息混成一个大包。

尤其要区分这几类：

- `全局知识`
  稳定背景，多个 Agent 可能共享

- `Agent 私有知识`
  某个 Agent 专属的方法、规则、经验

- `当前任务上下文`
  这次任务临时需要的信息

- `历史会话`
  最近交互、中间结论、待办、阶段状态

- `凭据 / auth`
  高敏感信息，必须最小暴露

### 2.6 每个 Agent 需要自己的信息边界

不同 Agent 的职责不同，因此不应该默认拥有同样的信息和环境。

例如：

- `researcher` 只需要读资料、写草稿
- `reviewer` 只需要读草稿并指出缺口
- `operator` 才可能需要更高执行权限

如果每个 Agent 默认共享同一套上下文、同一套 workspace、同一套权限，系统很容易退化成“多个名字的单 Agent”。

## 3. 这一阶段形成的判断标准

看到一个多 Agent 设计时，如果它默认让所有 Agent 都拿到：

- 完整业务背景
- 全部历史对话
- 所有执行记录
- 所有凭据

那通常不是“更聪明”，而是“没有边界”。

## 4. 第二阶段的最小模板：上下文分层模板

这个模板的作用，是帮助你定义某个 Agent 的信息应该怎么分层，而不是一股脑塞进去。

```md
# Context Layering

## Global Knowledge
所有相关 agent 都可以共享的稳定背景信息。

## Agent-Private Knowledge
只给某个 agent 的专属知识、方法、规则。

## Task Context
这次任务临时需要的信息。

## Session History
本次或近期会话中产生的中间结论、决策、待办。

## Credentials / Auth
敏感凭据，默认不共享，按需最小暴露。
```

## 5. 一个 PM 场景示例：researcher 的上下文分层

```md
# Agent: researcher

## Global Knowledge
产品基础背景
行业术语表
公开竞品资料规范

## Agent-Private Knowledge
研究方法
竞品分析模板
信息可信度判断规则

## Task Context
这次要研究的产品方向
目标用户群
本轮对比竞品名单

## Session History
已经查过哪些资料
初步结论
还缺什么信息

## Credentials / Auth
默认不给业务后台权限
默认不给外发权限
```

这个例子想说明的不是“researcher 应该写成这样”，而是：

- 有些信息应该所有相关 Agent 共享
- 有些信息只该 researcher 自己拥有
- 有些信息只在这次任务里临时带入
- 有些信息是运行时状态
- 有些信息默认不能给

## 6. 这一阶段和第一阶段模板的关系

第一阶段模板回答的是：

`一个 Agent 需要被定义清楚哪些维度。`

第二阶段模板回答的是：

`这些信息在进入 Agent 前，应该怎么分层、怎么暴露。`

所以二者关系是：

- 第一阶段偏 `Agent 定义`
- 第二阶段偏 `Context 组织`

这两层是互补的，不冲突。

## 7. 这一阶段是否通过的判断标准

如果你现在已经能稳定区分下面这些点，就算这一阶段过关：

- 上下文不是越多越好，而是越精准越好
- `workspace` 和 `context` 不是同一个概念
- `workspace` 更偏稳定边界，`context` 更偏单次请求实际注入的信息
- 业务知识、用户偏好、执行记录、凭据不该混在同一层默认共享
- 每个 Agent 需要独立的信息边界，而不只是换个名字

## 8. 下一阶段入口

第三阶段将进入：

`Tools、Sandbox、权限控制`

重点会从“信息边界”进一步推进到“能力边界”：

`多 Agent 的核心不只是多角色，而是不同 Agent 拥有不同能力和权限。`
