# 番外学习总结：Agent Runtime 与渐进式披露

## 1. 这两个概念的关系

先抓一句最重要的话：

`Agent runtime` 解决的是系统实际怎么跑起来。  
`渐进式披露` 解决的是信息、工具、权限和自治应该按什么节奏逐步开放。

所以二者不是并列概念，而是：

`runtime 是运行机制，渐进式披露是 runtime 中的核心边界控制原则。`

## 2. 什么是 Agent Runtime

可以先把 `agent runtime` 理解成一条持续循环：

1. 接收任务
2. 选择或激活合适的 Agent
3. 加载必要上下文
4. 大模型做推理，提出下一步动作
5. 系统检查工具、权限、sandbox、审批规则
6. 执行、拒绝或要求审批
7. 把结果返回给模型
8. 更新 session 状态
9. 判断是否继续下一步

这就是一个最小的 runtime。

## 3. 最小 Runtime 流程图

```text
用户任务
   |
   v
协调者 / 入口 Agent
   |
   v
选择 Agent
   |
   v
加载最小必要上下文
   |
   v
大模型推理
   |
   v
提出下一步动作
   |
   v
运行时检查
（工具 / sandbox / 权限 / 审批）
   |
   +---- 允许 ----> 执行工具 ----> 结果返回
   |
   +---- 申请 ----> 人工审批 ----> 允许 / 拒绝
   |
   +---- 拒绝 ----> 报错 / 回退 / 重新规划
   |
   v
更新会话状态
   |
   v
还需要下一步吗？
   |
   +---- 是 -----> 继续循环
   |
   +---- 否 -----> 输出结果 / 交接 / 结束
```

这个图表达的核心是：

- 大模型负责提出动作意图
- 程序/运行时负责判断能不能执行
- session 在每一轮执行中持续更新

## 4. 什么是渐进式披露

`渐进式披露` 不是一句口号，而是一套非常具体的系统设计原则：

- 不一次性把全部上下文塞给 Agent
- 不一次性把全部工具发给 Agent
- 不一次性把全部凭据暴露给 Agent
- 不一次性给它最高自治权限
- 先开放低风险价值，再按需要逐步升级

可以把它压缩成一句话：

`先给最小必要信息、最小必要工具、最小必要权限、最小必要自治，再按阶段升级。`

## 5. 一个 PM 场景例子

任务：

`帮我研究 3 个竞品，并产出一版 PRD 初稿`

系统里有 3 个 Agent：

- `generalist-pm`
  负责 intake、任务分类、分发、汇总

- `researcher`
  负责检索公开资料、竞品整理、研究结论收敛

- `spec-writer`
  负责根据研究结果起草 PRD

## 6. PM 场景运行流程图

```text
[用户提出任务]
   |
   v
[generalist-pm]
读取任务目标、已有背景、输出格式要求
   |
   v
判断需要两个子任务：
1. 竞品研究
2. PRD 起草
   |
   +----------------------------+
   |                            |
   v                            v
[researcher]                [spec-writer]
先拿到最小研究上下文            暂时不启动，或只拿到任务骨架
   |                            |
   v                            |
检索公开资料、整理结论           |
   |                            |
   v                            |
输出竞品分析草稿 ----------------+
                                |
                                v
                         [spec-writer]
                         现在再加载研究结果
                         起草 PRD
                                |
                                v
                         输出 PRD 草稿
                                |
                                v
                         [generalist-pm]
                         汇总并返回用户
```

这个例子表达的关键是：

- 不是所有 Agent 一开始都一起工作
- 不是所有 Agent 一开始都拿到全部上下文
- 不同 Agent 只在需要的阶段被激活

## 7. 渐进式披露主要发挥作用的地方

### 7.1 Agent 激活是渐进的

系统不会在用户一提任务时，就让所有 Agent 同时启动。

而是：

- 先由入口 Agent 判断任务类型
- 再只激活当前必要的 Agent

这减少了：

- 无关 Agent 的参与
- 不必要的信息暴露
- 协调复杂度

### 7.2 Context 加载是渐进的

例如 `researcher` 一开始只需要：

- 任务目标
- 竞品名单
- 输出格式要求

它不需要一开始就拿到：

- 全部历史会话
- 完整业务手册
- 所有用户偏好
- 内部敏感资料

而 `spec-writer` 只有在研究结果出来后，才需要进一步加载：

- 竞品分析结论
- PRD 模板
- 输出规范

这体现的是：

`先给完成当前步骤必需的信息，再按阶段补充。`

### 7.3 Tools 是渐进开放的

例如：

- `generalist-pm`
  只有分类、分发、汇总能力

- `researcher`
  有检索、读文件、写草稿能力

- `spec-writer`
  有读研究结果、写文档草稿能力

但它们默认都没有：

- 高风险执行权限
- 对外发送权限
- 正式落库权限

这说明工具不是一次性发满，而是按职责开放。

### 7.4 权限和凭据是渐进暴露的

例如 `researcher` 需要网页检索，不代表它需要：

- Jira token
- 业务后台账号
- 对外发送消息能力
- 正式发布权限

只有当某一步真的需要，而且风险可控时，系统才可能：

- 接受权限申请
- 进入审批
- 临时授予受限权限

这体现的是：

`不是“能力存在就默认可用”，而是“确有需要才暴露”。`

### 7.5 自治程度是渐进升级的

这是后续 Delegate Architecture 会系统展开的内容，但现在可以先建立一个基础认识。

同一个任务里，Agent 的自治通常可以分层：

- `Tier 1`
  读取 + 起草

- `Tier 2`
  代用户执行某些动作

- `Tier 3`
  主动触发和持续执行

在当前这个例子里，更合理的是：

- `researcher` 和 `spec-writer` 停留在 `Tier 1`
- 只输出研究草稿和 PRD 草稿
- 不默认代用户发送、发布或执行正式动作

这说明：

`系统应该先开放低风险价值，再决定是否开放高风险自治。`

## 8. 把 Runtime 和渐进式披露合起来看

下面这个流程图，把二者合在了一起：

```text
任务输入
  |
  v
选择最小必要 Agent
  |
  v
加载最小必要上下文
  |
  v
开放最小必要工具
  |
  v
在 sandbox 中运行
  |
  v
产出草稿 / 中间结果
  |
  v
是否需要更多上下文或权限？
  |
  +---- 否 ----> 继续执行 / 结束
  |
  +---- 是 ----> 请求升级 / 人工审批
                    |
                    v
              有限度地升级能力
```

这个图里，渐进式披露主要控制 4 件事：

- `Agent activation`
  谁先上场

- `Context loading`
  先给多少信息

- `Tool / permission release`
  先给多少能力

- `Autonomy upgrade`
  是否允许进一步自动执行

## 9. 一个反例：没有渐进式披露会怎样

如果没有渐进式披露，系统很容易变成这样：

```text
用户任务
   |
   v
所有 Agent 同时启动
所有 Agent 拿到全部上下文
所有 Agent 拿到全部工具
所有 Agent 拿到全部凭据
所有 Agent 都能自动执行
```

这种设计通常会快速暴露问题：

- 信息噪音过大
- Agent 角色失焦
- 权限过宽
- 风险难以审计
- 协调层演化成超级大脑
- 出错时难以回放和复盘

所以前面几阶段学到的：

- `workspace / context` 分层
- `tools / sandbox` 分层
- `权限按职责绑定`

本质上都在服务同一个原则：

`渐进式披露。`

## 10. 当前阶段可先记住的两个定义

`Agent runtime = Agent 从接收任务到调用工具、检查权限、更新状态、完成交付的整套运行循环。`

`渐进式披露 = 在 runtime 中，按任务阶段逐步开放上下文、工具、权限和自治，而不是一开始全部给满。`

## 11. 一句话总结

`runtime` 决定 Agent 怎么跑，`渐进式披露` 决定 Agent 跑的时候先给它多少世界、多少能力、多少自由。
