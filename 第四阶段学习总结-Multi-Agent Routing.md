---
layout: note
title: 第四阶段学习总结：Multi-Agent Routing
summary: 理解任务如何分发、结果如何回收，以及为什么协调层必须保持薄。
permalink: /multi-agent-routing/
---

# 第四阶段学习总结：Multi-Agent Routing

## 1. 这一阶段学到了什么

这一阶段的核心目标，是理解多 Agent 系统里最关键的协调问题：

- 任务由谁先接住
- 什么时候需要分发
- 应该分发给谁
- specialist 做完后结果怎么回收
- 系统下一步该继续、回退，还是结束

核心结论：

`Routing 解决的是协作分工问题，不是能力堆叠问题。`

再压缩一句：

`好的 routing 不是把所有任务串成固定流水线，而是基于当前缺口决定下一跳。`

## 2. 关键概念总结

### 2.1 Routing 解决的不是“谁更聪明”，而是“谁该负责”

只要系统里不止一个 Agent，就一定会出现协调问题：

- 谁先接任务
- 谁负责某一步
- 谁负责回收结果
- 谁决定是否继续下一步

如果没有 routing，系统通常会退化成两种坏形态：

- 一个超级总控 Agent 什么都做，最后还是单体系统
- 多个 Agent 彼此乱调，结果上下文、权限和职责都失控

所以 routing 的本质不是“多调几个 Agent”，而是：

`把协作边界定义清楚。`

### 2.2 Routing 不是固定流水线

很多人会把 routing 想成一条固定链路：

`generalist -> researcher -> writer -> reviewer -> generalist`

这看起来整齐，但风险很大，因为它默认：

- 每个任务都要经过所有 Agent
- 每一步都不能跳过
- 每一步都不能提前结束

更成熟的 routing 应该是：

- 可以有默认主路径
- 但不应该被固定流水线绑死
- 是否进入下一跳，要看当前产物缺什么

换句话说：

- 缺事实，回 `researcher`
- 缺表达组织，交 `writer`
- 缺审查挑战，交 `reviewer`
- 已经足够交付，就直接结束

这就是这一阶段最重要的判断之一：

`Routing 的关键是基于当前缺口做路径选择，而不是沿固定链路机械推进。`

### 2.3 入口 Agent 和 Specialist Agent 的职责不同

在一个最小的 PM 多 Agent 系统里，通常至少会有两层：

- `入口 Agent`
- `specialist Agent`

入口 Agent 更像协调层。

它通常负责：

- 识别任务类型
- 判断要不要分发
- 选择分发给谁
- 回收结果
- 决定下一步还是结束

specialist Agent 更像执行层。

它通常只负责某一类稳定职责，例如：

- `researcher`
  负责检索、整理、信息收敛

- `writer`
  负责结构化表达和文档起草

- `reviewer`
  负责 challenge、找缺口、提风险

这里最关键的是：

`入口层负责协调，不负责吞掉专家能力。`

### 2.4 结果回收也是 Routing 的一半

很多人以为 routing 只是在“前面怎么分发”，其实后半段同样重要：

`谁来收结果，按什么粒度收，收回来以后如何决定下一步。`

如果只会分发，不会回收，系统就会变成一堆平行 Agent 各干各的，最后没人知道：

- 哪个结果可以直接用
- 哪个结果只是草稿
- 哪个结果还缺信息
- 哪个结果应该进入下一位 specialist

所以 routing 实际上是一个闭环：

`识别 -> 分发 -> 执行 -> 回收 -> 判断是否继续 / 结束`

### 2.5 什么叫“薄协调层”

这一阶段最需要建立的概念，就是：

`协调层必须薄。`

所谓薄，不是说它不重要，而是说它只保留协调所必需的能力。

一个合格的协调层通常只负责：

- 快速识别任务类型
- 判断是否需要分发
- 决定交给哪个 specialist
- 回收并整理结果
- 判断流程是否收口

它不应该负责：

- 自己做研究
- 自己写稿
- 自己审稿
- 长期持有专家判断

原因很简单：

如果协调层开始同时承担研究、写作、审查和最终决策，它就会重新变成超级总控，multi-agent 也会坍缩回单体 Agent。

所以：

`薄协调层不是审美偏好，而是防止系统退化成单体的结构约束。`

### 2.6 新增 Agent、增加 Tool、优化现有 Agent，不是一回事

这三件事经常被混在一起，但必须分开判断。

#### 先优化现有 Agent

适用场景：

- 职责没变
- 任务类型没变
- 只是结果质量不稳定

例如：

- `researcher` 拿回的资料太散
- `writer` 的文风不稳定

这类问题通常更像：

- prompt 不够清晰
- 输出模板不够收敛
- 上下文组织有问题
- SOP 没定义好

#### 增加 Tool / Skill

适用场景：

- 角色职责没变
- 只是现有角色需要一种新能力

例如：

- `writer` 偶尔需要把 PRD 转成简单流程图

这更像能力扩展，而不是新职责出现。

一句话记忆：

`tool 解决“怎么做”。`

#### 新增 Agent

适用场景：

- 出现一类稳定重复的新职责
- 这类职责需要独立边界
- 它有自己的输入、输出和成功标准

例如：

- 每次上线前都需要固定做反方审查
- 每次重要输出前都需要独立 challenge

这类情况更适合新增 `reviewer` 之类的 specialist。

一句话记忆：

`agent 解决“谁负责”。`

### 2.7 Agent 应按职责和目标函数拆，而不是按动作切碎

判断一个角色是否值得拆成独立 Agent，不能只看它做了什么动作，而要看：

- 它的职责是否独立
- 它的成功标准是否独立
- 它是否应该和其他角色形成制衡

这里可以引入一个简单理解：

`目标函数 = 这个 Agent 在系统里最核心要优化什么。`

例如：

- `researcher`
  更关心信息是否充分、事实是否准确、材料是否覆盖关键面

- `writer`
  更关心表达是否清晰、结构是否顺、内容是否适合交付

- `reviewer`
  更关心有没有漏洞、有没有跳步、有没有风险和反例

所以 `writer` 和 `reviewer` 值得拆开，不是因为它们动作不同，而是因为它们追求的成功标准不同。

一句话总结：

`Agent 不是按动作拆，而是按职责和成功标准拆。`

## 3. 一个 PM 场景中的最小 Routing 闭环

任务：

`帮我研究 3 个竞品，并产出一版 PRD 初稿`

系统中有 4 个角色：

- `generalist-pm`
  负责 intake、分发、回收、汇总

- `researcher`
  负责竞品资料检索和信息收敛

- `writer`
  负责根据研究结果起草 PRD

- `reviewer`
  负责发现漏洞、补提风险、挑战结论

可以把最小 routing 闭环理解成这样：

```text
用户提出任务
   |
   v
generalist-pm
识别任务类型、判断是否拆分
   |
   v
分发给 researcher
   |
   v
researcher 返回竞品分析草稿
   |
   v
generalist-pm 回收结果并判断缺口
   |
   +---- 缺事实 ------------> 回 researcher 补资料
   |
   +---- 缺表达组织 --------> 交 writer 起草
   |
   +---- 缺审查挑战 --------> 交 reviewer 挑刺
   |
   +---- 已足够交付 --------> 汇总返回用户
```

这个例子表达的重点是：

- `generalist-pm` 不必自己做研究或写作
- 下一跳不由固定流程决定，而由当前缺口决定
- specialist 返回的是中间结果，不一定直接变成最终结果

## 4. 常见疑问：为什么不能让 Generalist 把专家能力一起做了

因为一旦这么做，系统很快就会出现这些问题：

- specialist 变成摆设
- 所有关键判断重新回到一个总控身上
- 流程变得难审计、难复盘
- 上下文和工具权限容易重新集中

表面上看，系统里仍然有多个 Agent；
但本质上，它已经变成：

`一个超级 Agent + 一堆外接插件`

这正是 multi-agent 设计最常见的反模式之一。

所以在设计 routing 时，要反复警惕：

`入口层只负责协调，不要让它顺手把专家活也干了。`

## 5. 这一阶段是否通过的判断标准

如果你现在已经能稳定区分下面这些点，就算这一阶段过关：

- Routing 解决的是协作分工问题，而不是能力堆叠问题
- routing 可以有默认路径，但不应该被固定流水线绑死
- 入口 Agent 和 specialist Agent 不是同一层职责
- 结果回收和下一跳判断，也是 routing 的核心部分
- 协调层应该薄，不能重新长成超级总控
- `新增 Agent`、`加 Tool`、`优化现有 Agent` 是三类不同问题
- Agent 更应该按职责和成功标准拆，而不是按动作机械切碎

## 6. 下一阶段入口

第五阶段将进入：

`Delegate Architecture`

重点会从“任务怎么分发”进一步推进到：

`Agent 的自治能力应该如何分层开放，哪些动作必须人审，哪些动作可以逐步自动化。`
