# 第一阶段学习总结：Agent 基础

## 1. 这一阶段学到了什么

这一阶段的目标，是先建立对 `Agent` 的正确理解，不急着看配置语法。

核心结论：

`Agent 不是一段人设 prompt，而是一个带边界、带状态、带工具、能执行任务的最小运行单元。`

更完整一点的定义：

`Agent = 在明确的指令上下文、工作空间、会话状态、工具能力和身份边界约束下运行的任务执行单元。`

## 2. 关键概念总结

### 2.1 Agent 不等于 Prompt

Prompt 只是 Agent 的一部分。
真正的 Agent 不只是“怎么说”，还包括：

- 它要做什么
- 它在哪做事
- 它能调用什么能力
- 它现在进行到哪一步
- 它能以什么身份做事

### 2.2 Workspace 是空间边界

`workspace` 更像 Agent 的工作场地或执行环境。

它决定：

- Agent 默认能读什么
- Agent 能不能写内容
- Agent 在哪个目录、知识范围或项目环境里工作

它不只是文件集合，而是执行边界的一部分。

### 2.3 Session 是时间边界

`session` 更接近短期工作记忆，而不是单纯的 todo list。

它通常包含：

- 当前任务进行到哪一步
- 最近几轮交互和中间结论
- 待办和下一步动作
- 当前这次运行里的阶段性状态

一句话记忆：

`workspace 是空间边界，session 是时间边界。`

### 2.4 Tools 决定能做什么

Agent 之所以不只是聊天，是因为它可能具备工具能力，例如：

- 读文件
- 写文件
- 执行命令
- 检索资料

工具能力不只是“增强功能”，它直接影响 Agent 是否真的能完成任务。

### 2.5 角色不等于身份边界

`角色` 讲的是职责分工。
`身份边界` 讲的是权限范围、可接触信息、是否能代表用户行动。

例如，两个都是 `reviewer`：

- 一个只能读草稿并提意见
- 另一个还能直接改稿或代用户发送反馈

它们角色接近，但身份边界不同。

## 3. 关于 AGENTS.md / SOUL.md / USER.md 的当前理解

这一阶段先形成一个分层认识，不要求记配置细节。

- `AGENTS.md`
  更偏工作规则、职责、协作约束、输入输出、handoff

- `SOUL.md`
  更偏稳定的行为原则、风格、长期人格特征

- `USER.md`
  更偏用户目标、偏好、禁忌、业务背景和个性化要求

它们都属于 Agent 的指令上下文，但不是同一种东西。

## 4. 关于“定义一个 Agent 要不要一次写全”的结论

不需要一开始把所有内容写满，但也不能完全边做边想。

更合理的方式是：

- 先定义最小必要集
- 再渐进细化
- 运行中再形成 session

其中：

- `职责、边界、workspace、工具权限、输入输出、handoff` 最好先明确
- `风格、细化规则、补充约束` 可以边做边完善
- `session` 本来就是运行时逐步形成的

所以，多 Agent 设计时，不是每个 Agent 都要写成一样厚的文档，而是每个 Agent 都应该经过同一套边界定义框架。

## 5. 初学者可直接使用的 Agent 最小模板

这个模板的作用是帮助你先把一个 Agent 设计清楚。
它是总蓝图，不等于必须直接对应某一个固定文件。

```md
# Agent: <name>

- Purpose:
- Do:
- Don't:
- Read Access:
- Write Access:
- Tools Allowed:
- Tools Denied:
- Identity:
- Input:
- Output:
- Handoff To:
- Human Approval Needed For:
- Session Notes:
```

### 各字段怎么理解

- `Purpose`
  这个 Agent 存在是为了解决什么问题

- `Do`
  它应该负责什么

- `Don't`
  它明确不负责什么

- `Read Access / Write Access`
  它默认能读写到什么范围

- `Tools Allowed / Tools Denied`
  它有哪些能力，哪些能力不能给

- `Identity`
  它以什么身份工作，能不能代表用户行动

- `Input / Output`
  它拿什么作为输入，产出什么结果

- `Handoff To`
  它完成后交给谁

- `Human Approval Needed For`
  哪些动作必须经过人审

- `Session Notes`
  运行时的当前状态、中间结论、待办和最近决策

## 6. 具体例子：generalist-pm

```md
# Agent: generalist-pm

- Purpose:
  作为总入口接收产品相关任务，判断任务类型，分发给合适的 specialist，并汇总结果给用户。

- Do:
  识别任务是研究、写作、评审还是信息整理。
  把任务拆成可交付的小块。
  分发任务并回收结果。
  向用户输出最终整理后的结果。

- Don't:
  不自己完成深度研究。
  不直接写完整 PRD。
  不替代 reviewer 做严格质疑。
  不执行高风险操作。

- Read Access:
  可以读取任务说明、项目背景、基础业务文档、已有产出。

- Write Access:
  只写汇总草稿、任务分发说明、最终整合结果。
  不直接改动核心业务资料。

- Tools Allowed:
  读取文件。
  基础搜索。
  任务拆解与结果汇总。

- Tools Denied:
  高风险执行工具。
  未审批的外部发送类操作。
  直接修改核心正式文档的权限。

- Identity:
  它是协调者，不是最终业务专家。
  不能以用户身份对外发送内容。
  默认只负责整理和分发，不负责拍板。

- Input:
  用户需求、任务目标、背景资料、已有文档。

- Output:
  任务分类结果。
  分发说明。
  汇总后的最终答复或草稿。

- Handoff To:
  researcher
  spec-writer
  reviewer
  或者交还给用户确认。

- Human Approval Needed For:
  对外发送。
  正式落库。
  高风险修改。
  涉及用户身份代表的动作。

- Session Notes:
  当前任务是什么。
  已经分发给谁。
  收到了哪些结果。
  还缺什么信息。
  下一步要不要交给人确认。
```

## 7. 这一阶段是否通过的判断标准

如果你现在已经能稳定区分下面几件事，就算这一阶段过关：

- `Agent` 不是人设 prompt
- `workspace` 不是背景说明，而是执行环境的一部分
- `session` 是运行中的短期状态，不是静态定义文件
- `角色` 和 `身份边界` 不是同一件事
- 一个 Agent 可以先定义最小可运行边界，再逐步细化

## 8. 下一阶段入口

第二阶段将进入：

`为什么不同 Agent 不应该默认共享全部上下文，以及 context / workspace 应该怎么分层。`
